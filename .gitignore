# Wrangler 로컬 캐시/임시 파일 — 배포할 때마다 자동 생성된다. 커밋 대상 아님.
.wrangler/
**/.wrangler/

# Node
node_modules/

# reading-server 런타임 데이터·빌드 산출물 — 학생 기록(db.json)은 절대 커밋 금지, dist는 CI가 조립
reading-server/data/
reading-server/dist/

# 운영 학생 원본 — 로컬 이관용, 저장소/Pages 배포 금지
.private/

# 구형 배포 스크립트가 만들던 관리자 비밀키 결과 파일 — 커밋 금지
sync/배포결과.txt
workbench/src/private-seed.json

# 교재 코칭 원문 — 공개 저장소에 두지 않는다(서버 KV로 이전, 2026-09). 로컬 사본은 개발용.
reading/textbook.json
